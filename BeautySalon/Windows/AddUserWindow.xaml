<Window x:Class="BeautySalon.Windows.AddUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавление/редактирование пользователя"
        Width="500"
        Height="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Информация о пользователе" 
                   FontSize="18" 
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <TextBlock Grid.Row="1" Text="Логин:" Margin="0,5"/>
        <TextBox x:Name="LoginBox" Grid.Row="2" Margin="0,0,0,15" Padding="5" Height="35"/>

        <TextBlock Grid.Row="3" Text="Пароль:" Margin="0,5"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="4" Margin="0,0,0,15" Padding="5" Height="35"/>

        <Grid Grid.Row="5" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Роль:" Margin="0,5"/>
            <ComboBox x:Name="RoleBox" Grid.Column="1" Margin="10,0,0,0" Height="35">
                <ComboBoxItem Content="Администратор" Tag="1"/>
                <ComboBoxItem Content="Пользователь" Tag="2" IsSelected="True"/>
            </ComboBox>
        </Grid>

        <!-- Чекбокс для блокировки/разблокировки -->
        <StackPanel Grid.Row="6" Orientation="Horizontal" Margin="0,0,0,15">
            <CheckBox x:Name="BlockCheckBox" 
                      Content="Заблокировать пользователя"
                      VerticalAlignment="Center"
                      Margin="0,5"/>
        </StackPanel>

        <TextBlock x:Name="ErrorText" Grid.Row="7" Foreground="Red" TextWrapping="Wrap"/>

        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Отмена" Width="100" Height="35" Margin="0,0,10,0" Click="CancelButton_Click"/>
            <Button Content="Сохранить" Width="100" Height="35" Background="#4CAF50" Foreground="White" Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Window>